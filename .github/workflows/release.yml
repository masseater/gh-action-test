name: release

on:
  workflow_dispatch:
    inputs:
      target:
        description: 'build, minor, or major'
        required: true
        default: 'build'

jobs:
  create-release:
    name: Create release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          path: master
      - name: Create release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ ./scripts/put_tag ${{ github.event.inputs.target }} }}